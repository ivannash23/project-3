<p>
  <strong>Title:</strong>
  <%= @task.title %>
</p>


<p>
  <strong>Date:</strong>
  <%= @task.date.strftime("%B %d, %Y") %>
  <%= @task.date.strftime("%H:%M") %>
</p>

<%= link_to 'Edit', edit_task_path(@task) %> |
<%= link_to 'Back', tasks_path %>
